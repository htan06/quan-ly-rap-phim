package com.application.view;

import java.awt.*;

public class ThemeConfig {
    // Vàng đậm hơn (Dark Goldenrod) để nhìn sang trọng trên nền trắng
    public static final Color PRIMARY = new Color(184, 134, 11);

    // Nền trắng tinh hoặc xám cực nhẹ (F8F9FA) để trông giống giao diện Web hiện đại
    public static final Color BACKGROUND = new Color(255, 255, 255);

    // Panel cùng màu nền hoặc hơi xám để tạo khối
    public static final Color PANEL = new Color(255, 255, 255);

    // Chữ chuyển sang màu đen hoặc xám đậm để dễ đọc trên nền trắng
    public static final Color TEXT = new Color(33, 37, 41);

    // Màu phụ cho các dòng mô tả (subtitle)
    public static final Color TEXT_LIGHT = new Color(108, 117, 125);

    // Màu viền (border) cho các ô input
    public static final Color BORDER = new Color(222, 226, 230);
}